# Signals

This directory is the recency layer for the council.

- `recent/*.md` are generated signal files per voice
- `summary.md` is the generated overview

These files are intentionally separate from `references/*.md`.

The profiles are hand-authored operating distillations.
The signals are short-horizon public activity snapshots.

Use both when a question depends on what a voice is emphasizing now.
